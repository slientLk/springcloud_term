spring:
  application:
    name: initial
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password:
    timeout: 5000
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1
        min-idle: 0
  profiles:
    active: 9091
  cloud:
    config:
      uri: http://localhost:8888